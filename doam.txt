 #include "DHT.h"
const int DHTPIN =2;
const int DHTTYPE = DHT11; //Khai báo loại cảm biến
 DHT dht (DHTPIN, DHTTYPE);
 void setup() {

Serial.begin(9600);
dht.begin(); // Khởi động cảm biến

void loop() {
float h =dht.readHumidity(); //Đọc độ ẩm
 float t =dht readTemperature();//Đọc nhiệt độ
 Serial.print("Nhiết do: "},
Serial.println(t);
Serial.print("Do am: ");
//Xuất độ ẩm
 Serial.println(h);
Serial.println();
delay(1000); 
}